// useProfile.ts
// Fase 10: Integración API
// Archivo generado según el plan de desarrollo

// Hook base (estructura, sin lógica interna)
// TODO: Implementar lógica de fetch, conexión con AuthProvider, manejo de estados y refresh en siguientes fases

export function useProfile() {
  // Estructura base, sin implementación
  // Retornar estados: loading, error, data, refresh
  return {
    loading: false,
    error: null,
    data: null,
    refresh: () => {},
  };
}
