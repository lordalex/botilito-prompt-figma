// AchievementsTab.tsx
// Fase 7: AchievementsTab
// Archivo generado según el plan de desarrollo

import React from 'react';
import { Achievement } from '../types';

export interface AchievementsTabProps {
  achievements: Achievement[];
}

// Componente base (estructura, sin UI ni lógica interna)
export const AchievementsTab: React.FC<AchievementsTabProps> = (props) => {
  // Estructura base, sin implementación UI
  return (
    <div>
      {/* Header: "X de Y logros completados" */}
      {/* Achievement Card: icono + nombre + PI reward badge */}
      {/* Descripción: texto explicativo del logro */}
      {/* Progress: barra + "X / Y" + "% completado" */}
      {/* TODO: Implementar UI según plan en siguientes fases */}
      <span>AchievementsTab (estructura base)</span>
    </div>
  );
}
